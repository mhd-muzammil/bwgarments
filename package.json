{
  "name": "bw-garments",
  "version": "1.0.0",
  "description": "Black & White Garments — Premium Fashion E-Commerce Platform (MERN Stack)",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "npx concurrently -n server,client -c blue,green \"npm run server\" \"npm run client\"",
    "start": "cd server && npm start",
    "seed": "cd server && npm run seed",
    "seed:categories": "cd server && node seed/seedCategories.js",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "cd server && npm test",
    "lint": "cd client && npm run lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
